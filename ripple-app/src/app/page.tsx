import FirebaseButtons from "@/components/FirebaseButtons";

export default function Home() {
  return (
    <section className="flex justify-center h-screen items-center">
      <FirebaseButtons />
    </section>
  );
}
